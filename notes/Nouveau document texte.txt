Ossature automatique == ! + ENTRER 
annotations selection == ctrl + :
. == class
$ == nom class
* == nombre de lignes
{} == nom de la ligne

git remote add origin https://github.com/Robinhood2red/CSS.git
git branch -m main
Ossature automatique = ! + ENTRER 
annotations selection = ctrl + :
. = class
$ = nom class
* = nombre de lignes
{} = nom de la ligne

POUR NOUVEAU PROJET --> Github --> Repository --> New --> "Suivre les indications"

git remote add origin https://github.com/Robinhood2red/CSS.git
git branch -M main
git push -u origin main


git add . == POUR maj SI problème
git commit -m "maj"
git push -u origin main

git pull origin main
git push origin main

SI CHANGEMENT ORDI :
cd /c/Users/louis/OneDrive/Bureau/Codage/AFPA
git clone https://github.com/Robinhood2red/"nomdudossier".git
code .

